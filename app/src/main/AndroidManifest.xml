<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srilankavehicleinfo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".InfoViewTwo"></activity>
        <activity android:name=".InfoViewOne" />
        <activity android:name=".ShowDetails" />
        <activity android:name=".SubmitNumber" />

        <activity android:name=".AlertNavigation"></activity>

        <activity android:name=".AlertProfilePage" />

        <activity android:name=".AlertUnsubsPage" />
        <activity android:name=".AlertSubsPage" />
        <activity android:name=".LicenseAlertMainPage" />
        <activity android:name=".MainActivity"></activity>


        <activity android:name=".LoginPage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".SignUp"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".PasswordResetMainPage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".PasswordResetSecondPage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        </activity>

        <activity android:name=".LoadingPage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>